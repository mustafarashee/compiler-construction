#include <iostream>
using namespace std;

class syntaxAnalyzer
{
  void Analyzer()
  {
    // linklist list;
    // Token tok;
    // list.print(&tok.retstart()) ;
    // *(this->start) = *start;
    //list.print(*start);
  }
  //private:
  linklist **start;
};